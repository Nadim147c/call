APP_NAME = call
VERSION ?= $(git describe --tags)

[default] build

[run]
cmd = go run main.go

[deps]
cmd = go get -v
cmd = go mod tidy

[build] deps
cmd = go build -o ${APP_NAME}

[install]
cmd = go install
