APP_NAME = call
VERSOIN ?= $(git describe --tags)

[default]
cmd = go build -v -o ${APP_NAME}

[deps]
cmd = go get -v
cmd = go mod tidy

[build]
cmd = go build -o ${APP_NAME}

[install]
cmd = go install
